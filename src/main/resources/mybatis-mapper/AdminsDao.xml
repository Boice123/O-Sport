<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jsj141.osport.dao.AdminsDao">

    <sql id="dynamicWhere">
        <where>
            <if test="_parameter.containsKey('ids') and ids != null and ids != ''">
                and id in (${ids})
            </if>
        </where>
    </sql>

    <select id="select" resultType="com.jsj141.osport.domain.Admins">
        select * from admins where id=#{id}
    </select>

    <select id="count" resultType="int">
        select count(*) from admins
        <include refid="dynamicWhere" />
    </select>

    <select id="list" resultType="com.jsj141.osport.domain.Admins">
        select * from admins
        <include refid="dynamicWhere" />
        <if test="_parameter.containsKey('order') and order!=null">
            order by ${order}
        </if>
        limit #{start},#{size}
    </select>

    <insert id="insert" parameterType="com.jsj141.osport.domain.Admins">
        insert into admins (loginName)
        values (#{loginName})
    </insert>

    <update id="update" parameterType="com.jsj141.osport.domain.Admins">
        update admins 
        <set>
            <if test="loginName != null">
                loginName=#{loginName},
            </if>
        </set>
        where id=#{id}
    </update>

    <update id="delete">
        delete from admins where id=#{id}
    </update>

</mapper>